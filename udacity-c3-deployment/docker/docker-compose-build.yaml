version: "3"
services:
  reverseproxy:
    build:
      context: .
    image: $DOCKER_USERNAME/reverseproxy
  backend_user:
    build:
      context: ../../udacity-c3-restapi-user
    image: $DOCKER_USERNAME/udacity-restapi-user
  backend_feed:
    build:
      context: ../../udacity-c3-restapi-feed
    image: $DOCKER_USERNAME/udacity-restapi-feed
  frontend:
    build:
      context: ../../udacity-c3-frontend
    image: $DOCKER_USERNAME/udacity-frontend
