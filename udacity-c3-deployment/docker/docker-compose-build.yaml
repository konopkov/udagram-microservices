version: "3"
services:
  reverseproxy:
    build:
      context: .
    image: konopkov/reverseproxy
  backend_user:
    build:
      context: ../../udacity-c3-restapi-user
    image: konopkov/udacity-restapi-user
  backend_feed:
    build:
      context: ../../udacity-c3-restapi-feed
    image: konopkov/udacity-restapi-feed
  frontend:
    build:
      context: ../../udacity-c3-frontend
    image: konopkov/udacity-frontend
